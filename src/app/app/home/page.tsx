import ContentRoot from "@/src/components/page-content/ContentRoot";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Quero-Quero | Início",
};

const Page = () => {
  return <ContentRoot>page</ContentRoot>;
};

export default Page;
